spring:
  application:
    name: ebooklib
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    contexts: schema
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
